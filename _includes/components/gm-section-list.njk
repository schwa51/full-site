{% gm %}
<hr>
<h3>GM content</h3>
{% set gmItems = (collections.system_content or [])
  | bysystem(system)
  | where("data.section", section)
  | where("data.gm", true)
  | sortBy("data.title") %}
{% if gmItems | length %}
  <ul class="list">
    {% for it in gmItems %}
      <li><a href="{{ it.url }}">{{ it.data.title }}</a></li>
    {% endfor %}
  </ul>
{% else %}
  <p><em>No GM entries yet for this section.</em></p>
{% endif %}
{% endgm %}
