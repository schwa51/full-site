---
title: Mothership
layout: layout.njk
theme: mothership
publish: true
system: mothership
no_heading_border: true
eleventyNavigation:
  key: Mothership
  parent: Home
  order: 1
---
<html>
<body>
<h2>SURVIVE. SOLVE. SAVE.   Pick One</h2>

<p><h3>Helpful Links</h3></p>
<p><a href="https://bit.ly/moshwes1">Learn how to play</a></p>
<p><a href="https://www.tuesdayknightgames.com/pages/mothership-companion-app">Official Mothership App</a></p>
<p><a href="/vault/systems/mothership/jobs/">View the <strong><u>Job Board</u></strong> for current postings →</a></p>
<p><a href="/vault/systems/mothership/bounty/">View the <strong><u>Bounty List</u></strong> for outstanding bounties →</a></p>

{% set system = systemSlug or (system | slug) %}
{% set sections = ["locations","sessions","items","npcs","lore","maps","general","characters","jobs"] %}

<section class="cards">
  {% for s in sections %}
    {# count only this system+section #}
    {% set count = (collections.public_content or [])
      | bysystem(system)
      | where("data.section", s)
      | length %}

    {% if count > 0 %}
      {# build the expected section index URL and check if it exists to avoid 404 #}
      {% set sectionIndexUrl = "/vault/systems/" + system + "/" + s + "/" %}
      {% set hasIndex = (collections.all or []) | where("url", sectionIndexUrl) | length > 0 %}

      <article class="card">
        <h3>
          {% if hasIndex %}
            <a href="{{ sectionIndexUrl }}">{{ s | capitalize }}</a>
          {% else %}
            {{ s | capitalize }}
          {% endif %}
        </h3>
      </article>
    {% endif %}
  {% endfor %}
</section>

{# Optional “featured” pins on the landing: tag pages with `index:echoes:landing` #}
{% set pins = (collections["index:" + system + ":landing"] or []) | sortBy("data.indexOrder") %}
{% if pins.length %}
  <h2>Featured</h2>
  <ul class="list">
    {% for p in pins %}
      <li><a href="{{ p.url }}">{{ p.data.title }}</a></li>
    {% endfor %}
  </ul>
{% endif %}

</body>
</html>

