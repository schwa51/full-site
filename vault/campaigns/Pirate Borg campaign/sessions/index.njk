---
title: Sessions
layout: layout.njk
type: sessions
theme: tor
publish: true
draft: false
campaign: Pirate Borg campaign
eleventyNavigation:
  key: sessions
  parent: Pirate Borg campaign
  order: 9
---

<section>
  <h1>{{ title }}</h1>
  <p>Welcome to the {{ title | lower }} section of the {{ campaign }} campaign.</p>

  {% set entries = collections.content | filterByMultiple({ type: "sessions", campaign: "Pirate Borg campaign", publish: true, draft: false }) %}
  <ul>
    {% for entry in entries %}
      <li><a href="{{ entry.url }}">{{ entry.data.title }}</a></li>
    {% else %}
      <li><em>No entries found.</em></li>
    {% endfor %}
  </ul>
</section>