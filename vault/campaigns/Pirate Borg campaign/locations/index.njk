---
title: Locations
layout: layout.njk
type: locations
theme: tor
publish: true
draft: false
campaign: Pirate Borg campaign
eleventyNavigation:
  key: locations
  parent: Pirate Borg campaign
  order: 5
---

<section>
  <h1>{{ title }}</h1>
  <p>Welcome to the {{ title | lower }} section of the {{ campaign }} campaign.</p>

  <ul>
    {% set entries = collections.locations | filterByMultiple({ campaign: "Pirate Borg campaign", publish: true, draft: false }) %}
    {% for entry in entries %}
      <li><a href="{{ entry.url }}">{{ entry.data.title }}</a></li>
    {% endfor %}
  </ul>
</section>