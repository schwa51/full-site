---
title: General
layout: layout.njk
type: general
theme: tor
publish: true
draft: false
campaign: Wildsea campaign
eleventyNavigation:
  key: general
  parent: Wildsea campaign
  order: 3
---

<section>
  <h1>{{ title }}</h1>
  <p>Welcome to the {{ title | lower }} section of the {{ campaign }} campaign.</p>

  <ul>
    {% set entries = (collections.general or []) | filterByMultiple({ campaign: "Wildsea campaign", publish: true, draft: false }) %}
    {% for entry in entries %}
      <li><a href="{{ entry.url }}">{{ entry.data.title }}</a></li>
    {% endfor %}
  </ul>
</section>