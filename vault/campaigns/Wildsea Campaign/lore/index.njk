---
title: Lore
layout: layout.njk
type: lore
theme: tor
publish: true
draft: false
campaign: Wildsea campaign
eleventyNavigation:
  key: lore
  parent: Wildsea campaign
  order: 6
---

<section>
  <h1>{{ title }}</h1>
  <p>Welcome to the {{ title | lower }} section of the {{ campaign }} campaign.</p>

  <ul>
    {% set entries = (collections.lore or []) | filterByMultiple({ campaign: "Wildsea campaign", publish: true, draft: false }) %}
    {% for entry in entries %}
      <li><a href="{{ entry.url }}">{{ entry.data.title }}</a></li>
    {% endfor %}
  </ul>
</section>