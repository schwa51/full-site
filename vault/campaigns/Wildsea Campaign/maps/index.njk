---
title: Maps
layout: layout.njk
type: maps
theme: tor
publish: true
draft: false
campaign: Wildsea campaign
eleventyNavigation:
  key: maps
  parent: Wildsea campaign
  order: 7
---

<section>
  <h1>{{ title }}</h1>
  <p>Welcome to the {{ title | lower }} section of the {{ campaign }} campaign.</p>

  {% set entries = collections.content | filterByMultiple({ type: "maps", campaign: "Wildsea campaign", publish: true, draft: false }) %}
  <ul>
    {% for entry in entries %}
      <li><a href="{{ entry.url }}">{{ entry.data.title }}</a></li>
    {% else %}
      <li><em>No entries found.</em></li>
    {% endfor %}
  </ul>
</section>