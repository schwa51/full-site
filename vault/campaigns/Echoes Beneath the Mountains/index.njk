---
title: Echoes Beneath the Mountains
layout: layout.njk
theme: tor
campaign: echoes
eleventyNavigation:
  key: Echoes
  parent: Home
  order: 1
---
<p>An original campaign set in the twilight years of the Third Age, as ancient secrets stir in the forgotten halls of Eregion and Moria.</p>

{% set campaign = "echoes" %}

/* Cards that link to each section */
<section class="cards">
  {% set sections = ["locations","sessions","items","npcs","lore","maps","general","characters"] %}

  {% for s in sections %}
    {% set items = collections["campaign:" + campaign] 
       | where("data.section", s) %}
    <article class="card">
      <h3><a href="/vault/campaigns/{{ campaign }}/{{ s }}/">{{ s | capitalize }}</a></h3>
      <p class="muted">{{ items | length }} entries</p>
    </article>
  {% endfor %}
</section>

<h1>Session logs</h1>
<ul>
  {% for entry in collections.public_sessions | byCampaign(data.campaign) %}
    <li><a href="{{ entry.url }}">{{ entry.data.title }}</a></li>
  {% endfor %}
</ul>
