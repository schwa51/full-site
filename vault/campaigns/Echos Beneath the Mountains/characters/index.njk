---
title: Characters
layout: layout.njk
type: characters
theme: tor
publish: true
draft: false
campaign: Echos Beneath the Mountains
eleventyNavigation:
  key: characters
  parent: Echos Beneath the Mountains
  order: 2
---

<section>
  <h1>{{ title }}</h1>
  <p>Welcome to the {{ title | lower }} section of the {{ campaign }} campaign.</p>

  {% set entries = collections.content | filterByMultiple({ type: "characters", campaign: "Echos Beneath the Mountains", publish: true, draft: false }) %}
  <ul>
    {% for entry in entries %}
      <li><a href="{{ entry.url }}">{{ entry.data.title }}</a></li>
    {% else %}
      <li><em>No entries found.</em></li>
    {% endfor %}
  </ul>
</section>