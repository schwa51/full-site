---
title: Npcs
layout: layout.njk
type: npcs
theme: tor
publish: true
draft: false
campaign: Mothership campaign
eleventyNavigation:
  key: npcs
  parent: Mothership campaign
  order: 8
---

<section>
  <h1>{{ title }}</h1>
  <p>Welcome to the {{ title | lower }} section of the {{ campaign }} campaign.</p>

  <ul>
    {% set entries = collections.npcs | filterByMultiple({ campaign: "Mothership campaign", publish: true, draft: false }) %}
    {% for entry in entries %}
      <li><a href="{{ entry.url }}">{{ entry.data.title }}</a></li>
    {% endfor %}
  </ul>
</section>