---
title: Items
layout: layout.njk
type: items
theme: tor
publish: true
draft: false
campaign: Mothership campaign
eleventyNavigation:
  key: items
  parent: Mothership campaign
  order: 4
---

<section>
  <h1>{{ title }}</h1>
  <p>Welcome to the {{ title | lower }} section of the {{ campaign }} campaign.</p>

  {% set entries = collections.content | filterByMultiple({ type: "items", campaign: "Mothership campaign", publish: true, draft: false }) %}
  <ul>
    {% for entry in entries %}
      <li><a href="{{ entry.url }}">{{ entry.data.title }}</a></li>
    {% else %}
      <li><em>No entries found.</em></li>
    {% endfor %}
  </ul>
</section>